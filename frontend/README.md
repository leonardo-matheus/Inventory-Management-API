# FrontEnd pertencente a API MoveMais.

* **React**
* **Material UI**

Para rodar a aplicação localmente:

1.  **Instalar os modulos**:
    ```bash
    npm install
    ```
2.  **Iniciar a aplicação**:
    ```bash
    npm run dev
    ```